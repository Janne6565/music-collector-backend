package com.musiccollector.model.core;

/** The four things worth telling somebody's friends about. */
public enum ActivityType {

    /** A copy added by hand, one at a time. Never an import. */
    COPY_ADDED,

    WISH_ADDED,

    /** "Off the wishlist, onto the shelf" — the line the design is proudest of. */
    WISH_FULFILLED,

    FRIENDSHIP_ACCEPTED
}
